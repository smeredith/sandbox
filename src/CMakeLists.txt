cmake_minimum_required (VERSION 3.10)
project (bigint_sample
                VERSION 1.0
                DESCRIPTION "A sample program that uses bigint.h"
                LANGUAGES CXX)

add_executable(a.out main.cpp)
